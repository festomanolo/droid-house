<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Compose draws the entire UI, so the Android theme only needs to get out of
      the way: no action bar, and a window background that matches the Compose
      surface so there is no flash between launch and first composition.
    -->
    <style name="Theme.DroidHouse" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- The consent shim must be invisible; only the system dialog should show. -->
    <style name="Theme.DroidHouse.Transparent" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

</resources>
